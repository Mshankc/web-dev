function validation(){
    const name=document.getElementById('name').nodeValue;
    const number=document.getElementById('name').nodeValue;
    const email=document.getElementById('name').nodeValue;
    const age=document.getElementById('age').nodeValue;

    document.write(name);
    document.write(number);
    document.write(email);
    document.write(age);
}
